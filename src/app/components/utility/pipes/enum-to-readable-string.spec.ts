import { EnumToReadableString } from './enum-to-readable-string';

describe('EnumToReadableString', () => {
  it('should create an instance', () => {
    expect(new EnumToReadableString()).toBeTruthy();
  });
});
