import { DefaultPointCloud } from './default-point-cloud';

describe('DefaultPointCloud', () => {
  it('should create an instance', () => {
    expect(new DefaultPointCloud()).toBeTruthy();
  });
});
